{% block form %}
    <form action="" method="POST">
        <h3>Select rounds count</h3>
        <input type="number" name="round">
        <button type="submit">submit</button>
    </form>
{% endblock %}

{% block table %}
    <style>
        table tr td{
            border: 1px solid black;
            padding: 3px;
            margin: 0;
            text-align: center;
        }
    </style>
    <h4>Category {{ sort }}</h4>



    {#{{ dump(database.3) }}#}
    <h5>{{ database.id}}</h5>
    <table>
    <tr style="background-color: gainsboro">
        <td>{{ 'id'|upper }}</td>
        <td>{{ 'class'|upper }}</td>
        <td>{{ 'name'|upper }}</td>
        <td>{{ 'vehicle'|upper }}</td>
        {% for item in database %}

            {#<td>round{{ item.roundcount1 }}time</td>#}


        {% endfor %}
        {#{% for item in database %}#}
            {#<td>ROUND {{ roundcount.roundcount }} TIME</td>#}
            {#<td>ROUND {{ roundcount.roundcount }} PENALTY</td>#}
            {#<td>ROUND {{ roundcount.roundcount }} FINAL TIME</td>#}
        {#{% endfor %}#}
        {% for item in database %}
            <td>ROUND {{ roundcount.roundcount }} TIME</td>
            <td>ROUND {{ roundcount.roundcount }} PENALTY</td>
            <td>ROUND {{ roundcount.roundcount }} FINAL TIME</td>
        {% endfor %}
        <td>BEST TIME</td>
    </tr>


        {% for item in database %}
        <tr>
            <td>{{ item.id }}</td>
            <td>{{ item.class }}</td>
            <td>{{ item.name }}</td>
            <td>{{ item.vehicle }}</td>
            {#{% if roundcount is define %}#}

            {% for roundcount in item %}
                <td>{{ roundcount.time }}</td>
                <td>{{ roundcount.penalty }}</td>
                <td>{{ roundcount.final_time }}</td>
            {% endfor %}

            {#{% endif %}#}

            <td><b>{{ item.best_time }}</b></td>

        {% endfor %}
        </tr>
</table>
{% endblock %}
